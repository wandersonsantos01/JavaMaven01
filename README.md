## Maven

### Commands

mvn [GOAL]

```shell
$ mvn clean
$ mvn compile
$ mvn package
$ mvn install
$ mvn test
$ mvn clean compile package
```

### Plugins

* jococo - Tests coverage


### Configure Proxy

Create and configure ~/.m2/settings.xml